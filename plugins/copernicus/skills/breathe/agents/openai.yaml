interface:
  display_name: "Breathe"
  short_description: "Deep investigation, distilled at human checkpoints"
  default_prompt: "Use $breathe to investigate this deeply and return only the checkpoint-complete brief."
